gui:
  showFileTree: false # for rendering changes files in a tree format
  showIcons: true
  nerdFontsVersion: "3"
git:
  diffRenderers:
    - colorArg: always
      command: delta --dark --paging=never
